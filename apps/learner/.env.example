# Fundi Learner — environment variables.
#
# POLICY: all env is SERVER-SIDE and injected at RUNTIME. There are NO
# `NEXT_PUBLIC_*` variables — nothing here is ever inlined into the client
# bundle. Read these only from `process.env` in a server context (server
# component, route handler, server action). A lint rule enforces this
# (packages/config/eslint/next.js); see packages/docs/features/0007.
#
# Local dev: copy to `.env.local` (gitignored) and fill in.
#   cp apps/learner/.env.example apps/learner/.env.local
# Deployed (Vercel): values come from the GitHub `dev` Environment and are
# applied as Vercel runtime env by the deploy workflow — never committed.

# Base URL of the Fundi API (server-to-server calls only, read in app/lib/bff.ts).
API_BASE_URL="http://localhost:3000"

# Cookie hardening for the auth session cookies (fundi_at / fundi_rt).
#   full → `__Host-` prefix + Secure (requires https; the correct prod value)
#   dev  → plain names, no Secure, so cookies set over plain-http localhost
# Unset defaults to `full` in production and `dev` otherwise. Set explicitly in
# staging/prod (https) to force full hardening regardless of NODE_ENV.
COOKIE_HARDENING="dev"
