// TODO: switch to '@fundi/config/eslint/next' once `next` is installed here
// (Sprint 0 Task 7) — eslint-config-next's parser requires the real `next`
// package to resolve, which doesn't exist yet at this scaffold stage.
import baseConfig from '@fundi/config/eslint/base';

export default [...baseConfig];
